#include <stdio.h>
#include "main.h"
/**
 * add - adds two integers
 *
 * @x: first argument of the func in int type
 *
 * @y: second argument of int type
 *
 * Return: the sum of two integers
 */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}

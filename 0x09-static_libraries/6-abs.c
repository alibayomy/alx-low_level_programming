#include <stdio.h>
#include "main.h"
#include <stdlib.h>
/**
 * _abs - computes the absolute value of an int
 *
 * @n: function paramter must be int
 *
 * Return: the absolute value of an int
 */
int _abs(int n)
{
	return (abs(n));
}

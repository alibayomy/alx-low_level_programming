#include <stdio.h>
#include "main.h"
#include <string.h>
#include <stdlib.h>
/**
 * _atoi - return the existing numbers into
 *	a given string
 *
 * @s: the string of the function
 *
 * Return: the numbers in the string
 */
int _atoi(char *s)
{
	int n;

	n = atoi(s);
	return (n);
}

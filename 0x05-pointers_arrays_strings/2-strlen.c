#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - return the length of a give string
 *
 * @s: given string
 *
 * Return: int represent the length of the string
 */
int _strlen(char *s)
{
	s_len = strlen(s);
	return (s_len);
}

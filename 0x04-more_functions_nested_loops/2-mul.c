#include <stdio.h>
#include "main.h"
/**
 * mul - get the value of multiply 2 integers
 *
 * @a: the first number
 *
 * @b: the second number
 *
 * Return: the sum of multiple of two integers
 */
int mul(int a, int b)
{
	int sum;

	sum = a * b;
	return (sum);
}
